package com.companytest.jsonplaceholderintegration

import com.companytest.jsonplaceholderintegration.domain.UserDomain
import com.companytest.jsonplaceholderintegration.model.User
import com.companytest.jsonplaceholderintegration.repository.FakeUserRemoteRepository
import com.companytest.jsonplaceholderintegration.repository.RemoteRepository
import kotlinx.coroutines.runBlocking
import org.junit.Assert
import org.junit.Test

class UserDomainTest {

    @Test
    fun retrieveAllUser_success(){
        /*
        //Arrange
        var repository: RemoteRepository<User> = FakeUserRemoteRepository()
        var userDomain: UserDomain = UserDomain(repository)

        runBlocking {
            //Act
            var users: ArrayList<User> = userDomain.retrieveUsers()

            //Assert
            Assert.assertEquals(1, users.size)
        }*/
    }
}